# MAST397FinalProjectFInal
The only thing that will be needed is to go to the original dataset (linked in the references of the report) and download the events.csv file as I had to remove it because git would not accept it, it was bigger than they allow.
Dependencies:
    pandas
    matplotlib
    seaborn
    numpy
    random